# URL base do backend (Spring Boot). Em produção precisa começar com https:// —
# o build falha (veja scripts/check-env.mjs) se não estiver definida ou se, em
# produção, não for https.
NEXT_PUBLIC_API_URL=http://localhost:8080
