# src/main/resources/static

Imagens estáticas (`images/`) usadas nos templates de e-mail.

As migrations SQL avulsas que ficavam aqui foram versionadas com Flyway — ver
`src/main/resources/db/migration/` (`V1`..`V11`, mesmo conteúdo, com guarda adicional por
existência de tabela para funcionar também num banco novo) e o item E-C1 no `AUDITORIA.md`.

## Subpacotes

- `images/` — imagens estáticas usadas nos templates de e-mail (ver `images/README.md`).
