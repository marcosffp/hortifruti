<?xml version="1.0"?>
<!DOCTYPE module PUBLIC
    "-//Checkstyle//DTD Checkstyle Configuration 1.3//EN"
    "https://checkstyle.org/dtds/configuration_1_3.dtd">

<module name="Checker">
    <!-- Encoding do projeto -->
    <property name="charset" value="UTF-8"/>

    <!-- Limite de linhas por arquivo -->
    <module name="FileLength">
        <property name="max" value="2000"/>
    </module>

    <!-- Comprimento de linha -->
    <module name="LineLength">
        <property name="max" value="120"/>
        <property name="ignorePattern" value="^package|^import"/>
    </module>

    <!-- Importações e regras que analisam código-fonte -->
    <module name="TreeWalker">

        <!-- Nome de pacotes deve ser minúsculo -->
        <module name="PackageName"/>

        <!-- Não permitir imports não usados -->
        <module name="UnusedImports"/>

        <!-- Ordem dos imports: java.*, javax.*, depois os outros -->
        <module name="ImportOrder">
            <property name="groups" value="java,javax,org,spring,com"/>
            <property name="ordered" value="true"/>
            <property name="separated" value="true"/>
        </module>

        <!-- Regras de indentação -->
        <module name="Indentation">
            <property name="basicOffset" value="4"/>
            <property name="tabWidth" value="4"/>
        </module>

        <!-- Espaçamento -->
        <module name="WhitespaceAround"/>
        <module name="WhitespaceAfter"/>

        <!-- Chaves obrigatórias -->
        <module name="NeedBraces"/>

        <!-- Nomes de variáveis, métodos e classes -->
        <module name="LocalVariableName"/>
        <module name="MethodName"/>
        <module name="ParameterName"/>
        <module name="MemberName"/>
        <module name="ClassTypeParameterName"/>
        <module name="MethodTypeParameterName"/>
        <module name="InterfaceTypeParameterName"/>

        <!-- Evitar blocos vazios -->
        <module name="EmptyBlock"/>

        <!-- Nome do arquivo deve bater com a classe pública -->
        <module name="OuterTypeFilename"/>
    </module>
</module>
